---
description: Update the .agent/AI_CONTEXT.md file with the latest project structure and architectural changes.
---

1. Read the current content of `.agent/AI_CONTEXT.md` to understand the existing structure.
2. Read `README.md`, `package.json`, and list the directory structure of `src` (components, pages/app, styles) to gather current project state.
3. Compare the current project state with the content of `.agent/AI_CONTEXT.md`.
4. If there are discrepancies (new libraries, changed folder structure, new features), update `.agent/AI_CONTEXT.md` to reflect the reality.
5. Ensure the "Roadmap" section is preserved unless specifically instructed to change it.
6. Verify the file is formatted correctly as Markdown.
