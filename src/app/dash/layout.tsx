export default function DashLayout({
  children,
}: {
  children: React.ReactNode;
}) {
  // Placeholder dashboard layout. Will be replaced with actual dashboard in Phase 2
  return (
    <div className="min-h-screen bg-black text-white">
      {children}
    </div>
  );
}
